import {PXtoViewHeightPipe} from './pxto-view-height.pipe';

describe('PXtoViewHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new PXtoViewHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
