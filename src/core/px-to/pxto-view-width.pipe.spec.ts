import {PXtoViewWidthPipe} from './pxto-view-width.pipe';

describe('PXtoViewWidthPipe', () => {
  it('create an instance', () => {
    const pipe = new PXtoViewWidthPipe();
    expect(pipe).toBeTruthy();
  });
});
